<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/page_margin"
    tools:context=".activity.ScanLoginActivity">

    <RelativeLayout
        android:id="@+id/login_web_wrapper"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_above="@+id/web_login_check"
        android:layout_centerHorizontal="true"
        android:background="@drawable/progress_bg"
        android:visibility="invisible">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="@string/login_succeed"
            android:textAlignment="center" />
    </RelativeLayout>

    <TextView
        android:id="@+id/web_login_check"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="@string/web_login_check"/>

    <Button
        android:id="@+id/check_login"
        android:layout_below="@id/web_login_check"
        android:layout_centerHorizontal="true"
        android:layout_width="match_parent"
        android:layout_height="@dimen/button_height"
        android:layout_margin="@dimen/button_margin"
        android:background="@drawable/btn_selector"
        android:textColor="@android:color/white"
        android:textSize="@dimen/button_text_size"
        android:text="@string/check_login" />

    <Button
        android:id="@+id/cancel_login"
        android:layout_below="@id/check_login"
        android:layout_centerHorizontal="true"
        android:layout_width="match_parent"
        android:layout_height="@dimen/button_height"
        android:layout_margin="@dimen/button_margin"
        android:background="@drawable/btn_selector"
        android:textColor="@android:color/white"
        android:textSize="@dimen/button_text_size"
        android:text="@string/cancel_login" />
</RelativeLayout>